# 服务器配置
PORT=3000
NODE_ENV=production

# JWT 密钥（生产环境请修改为随机字符串）
JWT_SECRET=your-super-secret-key-change-this

# 数据库路径
DB_PATH=./server/data/hello-ai.db

# CORS 允许的源（生产环境设置为你的域名）
CORS_ORIGIN=*

# OpenAI API Key（可选，不配置则使用本地模拟响应）
# OPENAI_API_KEY=sk-xxx
